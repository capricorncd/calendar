```jsx
import { createSignal } from "solid-js";
import { ZxSolidCalendar } from "../src";

export default function Normal() {
  const [date, setDate] = createSignal()

  return (
    <div>
      <p>value {date()}</p>
      <div class="example-demo-wrapper">
        <ZxSolidCalendar
          footer={<div>insert a footer</div>}
          value={date()}
          change={(newDate) => {
            setDate(newDate)
          }}
        />
        INJECT_CODE
      </div>
    </div>
  )
}
```