```jsx
import React, { Component } from 'react'
import { ZxReactCalendar } from '../../index'

class App extends Component {
  constructor(props) {
    super(props)
    this.state = {
      date: '1999/09/09'
    }
  }

  handleChange(res, original) {
    this.setState({
      date: res
    })
  }

  render() {
    const footer = <div style={{fontSize: '12px', color: '#999', textAlign: 'center', paddingBottom: '10px'}}>
      © 2020 capricorncd.
    </div>
    return <>
      <h2>footer</h2>
      <p>value: {this.state.date}</p>
      <div className="example-demo-wrapper">
        <ZxReactCalendar
          footer={footer}
          value={this.state.date}
          change={(res, original) => { this.handleChange(res, original) }}
        />
        INJECT_CODE
      </div>
    </>
  }
}

export default App
```
